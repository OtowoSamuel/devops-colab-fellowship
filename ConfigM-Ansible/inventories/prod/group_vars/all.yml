---
# Additional environment-based host variables can be placed here  
# These variables will be applied to all hosts in the production environment

# Example host-specific overrides:
# web-prod-01:
#   nginx_worker_processes: 8
#   nginx_worker_connections: 4096
#
# db-prod-01:
#   mysql_max_connections: 500
#   mysql_innodb_buffer_pool_size: 2G
