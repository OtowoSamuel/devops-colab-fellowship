# allowed-services.conf

# This file contains a list of allowed services and ports for the firewall configuration.
# Format: service_name port/protocol

# Allow SSH
ssh 22/tcp

# Allow HTTP
http 80/tcp

# Allow HTTPS
https 443/tcp

# Allow DNS
dns 53/udp

# Allow NTP
ntp 123/udp

# Allow custom application (example)
# myapp 8080/tcp